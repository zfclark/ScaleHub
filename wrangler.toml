# Cloudflare 部署配置（Workers 静态资产方式）
# Cloudflare 构建环境：Build command = npm run build:cf，Deploy command = npx wrangler deploy
name = "scalehub"
compatibility_date = "2026-09-15"

[assets]
directory = "./dist"
